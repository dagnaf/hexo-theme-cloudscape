<% if (post.tags && post.tags.length){ %>
  <div class="article-tag <%= post.categories && post.categories.length ? '' : 'u-max-full-width' %>">
    <i class="fa fa-tags"></i>
    <%- list_tags(post.tags, {
      show_count: true,
      class: 'article-tag'
    }) %>
  </div>
<% } %>
